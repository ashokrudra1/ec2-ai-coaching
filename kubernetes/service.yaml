apiVersion: v1
kind: Service
metadata:
  name: ai-coach-service
  labels:
    app: ai-coach
spec:
  selector:
    app: ai-coach
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    name: http
